<!DOCTYPE html><html><head>
    <meta name="twitter:card" content="summary" />
    <meta property="og:title" content="{{ name }}" />
    <meta property="og:description" content="{{ description }}" />
    <meta property="og:image" content="https://cdn.discordapp.com/app-icons/{{ id }}/{{ icon }}.png" />
    <meta property="og:url" content="{{ redirect_link }}" />
    <style>
        body {
            display: flex;
            height: 100vh;
            width: 100vw;
            justify-content: center;
            margin: 0;
            align-items: center;
            overflow: hidden;
        }
        a {
            width: 100%;
            text-align: center;
            font-size: 2em;
            padding: calc(50% - 6em) 0;
        }
    </style>
</head><body>
    <a href="{{ redirect_link }}">Click anywhere to be redirected to the invite page.</a>
</body></html>
